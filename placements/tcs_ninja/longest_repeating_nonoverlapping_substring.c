/*
 * find 2 identical substrings of max len which don't overlap
 * if there exists more than one, return any of them
 * eg: input is fourtyfour
 * 	output: four is an identical substring of max len
 */
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main()
{
	char input[100];
	scanf("%s", input);
	return 0;
}
